import { Button, Stack, Typography } from '@mui/material'

export default function Page() {
  return (
    <Stack spacing={16}>
      <Button variant="contained">Login</Button>
      <Typography>Powered by SendingNetwork</Typography>
    </Stack>
  )
}
