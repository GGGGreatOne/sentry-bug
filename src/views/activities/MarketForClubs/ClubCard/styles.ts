import { SxProps } from '@mui/material'

export const styles = {
  root: {},
  header: {},
  actions: {
    m: '0 20px 20px 20px',
    p: 16,
    minHeight: 132,
    borderRadius: '12px',
    background: 'var(--ps-black)'
  }
} as Record<string, SxProps>
